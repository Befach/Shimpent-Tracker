specVersion: "v1beta"
serviceId: "finalfinal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finalfinal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
